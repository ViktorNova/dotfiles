# This file has been auto-generated by i3-config-wizard(1).
# And heavilly customized by Viktor Nova.
# It will not be overwritten, so edit it as you like.

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Mod4 is the Windows key
set $mod Mod4

###################
####  COLORS   ####
###################
# class                 border  backgr. text    indicator
client.focused          #0055ff #0055ff #000000 #2e9ef4
client.focused_inactive #0022ff #0055ff #000000 #484e50
client.unfocused        #000070 #111111 #0055ff #0800ab
client.urgent           #aaff00 #900000 #ffffff #900000

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. 
font pango:Enter Sansman 12

#Fat borders for easier use on touchscreens
# DISABLED FOR OLDER i3
new_window normal 6

# Hide borders that touch the edge (to save space)
hide_edge_borders both

###################

bindsym Ctrl+Escape exec menu-standalone
bindsym Ctrl+Mod1+l exec i3lock



###################
#### ALT+TAB E ####
###################
bindsym $mod+Tab focus down
bindsym $mod+Shift+Tab focus up

###################
####MUSIC BINDS####
###################
#Hotkeys
# Volume
#bindsym XF86AudioRaiseVolume exec amixer -q set Master 2dB+ unmute
#bindsym XF86AudioLowerVolume exec amixer -q set Master 2dB- unmute
#bindsym XF86AudioMute exec amixer -q set Master toggle

# Print Screen
bindsym Print exec xfce4-screenshooter

# Startup Applications
exec --no-startup-id xrandr --output DP-3 --rotate left  
exec --no-startup-id mount /dev/sdb5
exec --no-startup-id mount /dev/sdc1
exec --no-startup-id copyq
exec --no-startup-id nm-applet
exec --no-startup-id qterminal 
exec --no-startup-id remmina
exec --no-startup-id x2goclient
exec --no-startup-id launchy
exec --no-startup-id cadence
exec --no-startup-id znotes
exec --no-startup-id piedock
exec --no-startup-id qasmixer -t
exec --no-startup-id export XDG_CURRENT_DESKTOP=gnome 
#exec --no-startup-id xfce4-panel


# Ctrl + Alt + Left/Right to switch workspaces
bindsym $mod+Ctrl+Left workspace prev
bindsym $mod+Ctrl+Right workspace next	

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -fn 'Enter Sansman-16' -nf '#0055ff' -nb '#000000' -sb '#00ffff' -sf '#000000' 
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1 HOME SCREEN
bindsym $mod+2 workspace 2 REMOTE     
bindsym $mod+3 workspace 3 CRM        
bindsym $mod+4 workspace 4 SEQUENCER  
bindsym $mod+5 workspace 5 PROXMOX    
bindsym $mod+6 workspace 6 AWESOME    
bindsym $mod+7 workspace 7 WEB        
bindsym $mod+8 workspace 8 FILES      
bindsym $mod+9 workspace 9 DEV        
bindsym $mod+0 workspace 10 EXTERNAL  

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1 HOME SCREEN
bindsym $mod+Shift+2 move container to workspace 2 REMOTE     
bindsym $mod+Shift+3 move container to workspace 3 CRM        
bindsym $mod+Shift+4 move container to workspace 4 SEQUENCER  
bindsym $mod+Shift+5 move container to workspace 5 PROXMOX    
bindsym $mod+Shift+6 move container to workspace 6 AWESOME    
bindsym $mod+Shift+7 move container to workspace 7 WEB        
bindsym $mod+Shift+8 move container to workspace 8 FILES      
bindsym $mod+Shift+9 move container to workspace 9 DEV        
bindsym $mod+Shift+0 move container to workspace 10 EXTERNAL  

# Allow popups during full screen by taking you out of full screen automatically
popup_during_fullscreen leave_fullscreen

# START APPS ON SPECIFIC WORKSPACES
# (WORK STUFF):
assign [title="Remmina Remote Desktop Client"]          2 REMOTE     
assign [title="X2Go Client"]           			            2 REMOTE     
assign [title="WIS"]                             2 REMOTE     


assign [class="Qmidiroute"]             1 MIDI
for_window [title="qtpanel"] output DP-3
assign [title="qtpanel"]		2 REMOTE      

assign [class="Non-Session-Manager"]    1 SESSION     
assign [class="Claudia.py"]		1 SESSION     
assign [class="Catia.py"]		1 SESSION     
#assign [class="carla"]			2 PLUGINS / FX
#assign [class="Carla"]			2 PLUGINS / FX
#assign [class="Hydrogen"]               3 DRUM MACHINE
assign [class="Qtractor"]               4 SEQUENCER   
assign [class="Non-Sequencer"]		4 SEQUENCER   
assign [class="Muse"]			4 SEQUENCER   
assign [class="Los-2014"]               4 SEQUENCER   
assign [class="Oomidi"]			4 SEQUENCER   
assign [class="dino"]			4 SEQUENCER   
assign [class="Dino"]			4 SEQUENCER   
assign [class="muse"]               	4 SEQUENCER   
assign [class="Qmidiarp"]              	5 ARPEGGIATOR 
assign [class="Qlcplus"]              	7 DMX LIGHTING
assign [class="Pcmanfm"] 		8 FILE MANAGER



# NEW WORKSPACE DEFAULTS
default_orientation vertical
workspace_layout stacking

# APP-SPECIFIC WINDOW SETTINGS
for_window [class="Hydrogen"] 					mode stacking

# OpenOctave MIDI / LibreOctave Studio
for_window [class="Los-2014"] 				  	floating disable;mode stacking
for_window [title="LOS: Edit Controller Event"] 		floating enable
for_window [title="LOS: Transport"] 				floating enable
for_window [title="OOMidi: Transport"] 				floating enable
for_window [title="LOS: Audio/Midi Connections Manager"] 	floating enable
for_window [class="winamp.exe"]         			floating enable

for_window [class="Claudia.py"] 				floating disable;mode stacking
#for_window [class="carla"]		 			floating disable;mode stacking
#for_window [class="Carla"]			 		floating disable;mode stacking
for_window [class="Calf_gtk"]	 				floating disable;mode stacking
for_window [class="Wine"] 					floating disable

# SYSTEM 
for_window [title="gimp-image-window"] 				floating enable; 1pixel
for_window [title="tilda"] 					floating enable; normal 6px
for_window [title="zNotes"] 					floating enable; normal 6px


# Scratchpad
for_window [class="Qterminal"] move scratchpad
bindsym F12 scratchpad show
 

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"



### PUT SHADOWS ON FLOATING WINDOWS

set $float exec --no-startup-id path_to/ugly_workaround_script.sh, floating toggle

# toggle tiling / floating
#bindsym $mod+Shift+space floating toggle
bindsym $mod+Shift+space $float

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle
bindsym $mod+space $float


#for_window [class="Nm-connection-editor"] border pixel

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	id externalBar
        font pango:Enter Sansman 20
	position bottom
	output DP-3
	tray_output 	none
	workspace_buttons yes

 colors {
        focused_workspace       #000000 #00ffff
        inactive_workspace      #00ffff #000000
        active_workspace        #000000 #0800ab
        urgent_workspace        #000000 #0055ff
        }

#	strip_workspace_numbers yes # Doesn't work on older i3
#	status_command py3status -c ~/.i3/i3status.conf
#	status_command i3status
#	status_command i3blocks
	status_command i3situation


}

bar {
	id bottom-bar
	position bottom
	tray_output DP-2
  output DP-2
  
	status_command py3status

	font pango:Enter Sansman 16

 colors {
	focused_workspace       #000000 #00ffff
	inactive_workspace 	#00ffff #000000
	active_workspace        #000000 #0800ab
	urgent_workspace        #000000 #0055ff 
	}

}


